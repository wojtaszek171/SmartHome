import enUSStrings from './strings/en-US.json';
import plStrings from './strings/pl-PL.json';

const locales = {
  'en-US': { common: enUSStrings },
  en: { common: enUSStrings },
  pl: { common: plStrings }
};

export default locales;
